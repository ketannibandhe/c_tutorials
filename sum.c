#include<stdio.h>
#include<math.h>
int main(){
    int a,b,c,power;
    /*declare the variable before using it
    int x=y=z=4 is in valid because 
    we cannot declare and use the variable in the same statement
    here we have assigned value 4 to z and value of z to y and further to x
    but z is undeclared before using it */
    // printf("enter number1\n");
    // scanf("%d",&a);
    // printf("enter number2\n");
    // scanf("%d",&b);
    // // power= pow(a,b);
    // printf("the sum of %d and %d is %d",a,b,a+b);
    // return 0;
}